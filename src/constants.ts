// used in the UI to create the Combobox with actions
export const FETCH_SHORTCUTS_EVENT = "fetchAllShortcutsByFolder";

// special "All shortcuts" folder ID
export const NONE_FOLDER_ID = "none";

// used in the CLI commands to also list the shortcut and folder identifiers
export const SHOW_IDENTIFIERS = "--show-identifiers";
